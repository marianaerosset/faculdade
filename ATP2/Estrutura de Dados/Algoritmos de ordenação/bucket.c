//professor passou apenas o pseudocódigo
/*
BucketSort(A, n)
    para i ← 0 até n − 1 faça
        B[i] ← NIL (nulo)
    para i ← 1 até n faça
        INSIRA(B[⌊n ∗ A[i]⌋], A[i])
    para i ← 0 até n − 1 faça
        ORDENELISTA(B[i])
    C ← CONCATENE(B, n)
    retorne C

• INSIRA(p, x): insere x na lista apontada por p
• ORDENELISTA(p): ordena a lista apontada por p
• CONCATENE(B, n): devolve a concatenação das listas apontadas por B[0..n − 1]
*/

void bucketSort(int A[], int n){
    for(int i = 0; i < n, )
}